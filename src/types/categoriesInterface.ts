export interface categoriesData {
  currentPage: number
  numberOfPages: number
  limit: number
}

export interface categories {
  _id: string
  name: string
  slug: string
  image: string
  createdAt: string
  updatedAt: string
}